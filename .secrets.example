# .secrets.example - Local secrets for ACT (GitHub Actions runner)
#
# Copy this file to .secrets and fill in your values:
#   cp .secrets.example .secrets
#
# IMPORTANT: .secrets is in .gitignore (never commit real secrets!)

# E2E Test Org Authentication URL
# Get this from: sf org display --target-org your-org --verbose --json | jq -r '.result.sfdxAuthUrl'
SF_AUTH_URL_E2E_ORG=force://PlatformCLI::REFRESH_TOKEN@INSTANCE_URL

# Optional: Other secrets for future use
# SALESFORCE_CLIENT_ID=your-client-id
# SALESFORCE_CLIENT_SECRET=your-client-secret

